export const createRouteController = (req, res) => {
  /*
    escreva esse controller
    1 receber um token bearer via header e validar esse token em uma determinada rota
    2 receber um payload com um array de items
    3 validar todos esses itens fazendo um get em uma rota http
    4 criar o pedido com status recebido
    5 criar um historico dizend que o pedido foi recebido
    6 retornar que o pedido foi recebido
  */
  return res.send('salve');
};
