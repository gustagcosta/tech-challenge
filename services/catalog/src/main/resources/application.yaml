server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/docker?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: docker
    password: docker

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect